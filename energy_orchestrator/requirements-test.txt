# Test dependencies for Energy Orchestrator
# Install with: pip install -r requirements-test.txt

# Core testing framework
pytest==8.3.4
pytest-cov==6.0.0
pytest-timeout==2.3.1

# Test utilities
pytest-mock==3.14.0
pytest-xdist==3.6.1  # For parallel test execution

# Optional: for better output formatting
pytest-sugar==1.0.0
